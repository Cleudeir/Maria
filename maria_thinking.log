Thinking Process:

1.  **Analyze the Request:**
    *   **Role:** Expert Prompt Engineer.
    *   **Task:** Improve the user's prompt ("create game snake html") to be clear, detailed, precise, and structured.
    *   **Constraints:** Ensure all requirements, edge cases, and testing strategies are explicit. Keep the original intent intact.
    *   **Output Format:** Provide *only* the improved task prompt. No preamble, no XML tags.

2.  **Analyze the Original Prompt:**
    *   **Intent:** Create a Snake game.
    *   **Technology:** HTML (implies HTML, CSS, JavaScript).
    *   **Complexity:** Very low (just "create game snake html").
    *   **Missing Details:** Game logic (movement, eating, growing, collision), UI design, controls, scoring, responsiveness, code structure, error handling, testing, etc.

3.  **Determine Improvements:**
    *   **Context & Tech Stack:** Specify HTML5 Canvas or DOM manipulation, CSS for styling, ES6 JavaScript for logic.
    *   **Core Mechanics:** Grid-based movement, food generation, snake growth, collision detection (walls, self), score tracking.
    *   **UI/UX:** Start/Pause/Reset buttons, score display, responsive design (mobile-friendly?).
    *   **Code Structure:** Modular, commented, clean code.
    *   **Edge Cases:** Game over conditions, input buffering (preventing multiple key presses in one frame), food on snake body.
    *   **Testing/Validation:** How to verify functionality (e.g., run locally, specific test scenarios).

4.  **Drafting the Improved Prompt:**
    *   *Goal:* Act as the prompt engineer creating the *task* for the AI to execute.
    *   *Structure:* Role, Task, Requirements, Constraints, Output Format.
    *   *Content:*
        *   Build a fully functional Snake game.
        *   Use HTML5 Canvas for rendering.
        *   Implement game loop, input handling, collision logic.
        *   Add score tracking.
        *   Ensure code is clean and commented.
        *   Handle edge cases (walls, self-collision, input lag).
        *   Include instructions on how to run/test.

5.  **Refining for "Prompt Optimization" Standards:**
    *   Needs to be a prompt *for* an AI to generate the code.
    *   Must be explicit about the output (single HTML file or separated files?). Let's assume a single file for simplicity unless specified, but modern best practice is separation. Let's aim for a single HTML file containing CSS and JS for ease of testing by the user, as per the original "create game snake html" request.
    *   Needs to specify the game loop speed, grid size, etc.
    *   Needs to specify visual style (colors, background).

6.  **Finalizing the Prompt Text:**
    *   Start with a clear objective.
    *   Detail the technical requirements (HTML/CSS/JS).
    *   Detail the game logic (movement, food, score, game over).
    *   Detail the UI requirements.
    *   Detail the edge cases (input handling, collision).
    *   Detail the output format (complete, runnable code).

7.  **Review against Constraints:**
    *   No preamble.
    *   No XML tags.
    *   Just the prompt.

8.  **Drafting the Final Output:**
    "Act as a Senior Frontend Developer. Your task is to create a fully functional, self-contained HTML5 Snake game using a single HTML file that includes embedded CSS and JavaScript.

    **Core Requirements:**
    1.  **Rendering:** Use the HTML5 Canvas API for high-performance rendering.
    2.  **Game Logic:**
        *   Implement a grid-based system (e.g., 20x20 grid).
        *   Snake movement controlled by arrow keys or WASD (prevent reversing direction directly).
        *   Food generation at random grid coordinates (ensure it does not spawn on the snake).
        *   Snake growth mechanism upon consuming food.
        *   Collision detection for walls and self-intersection to trigger Game Over.
        *   Score tracking (display current score and high score).
    3.  **User Interface:**
        *   Clean, modern styling with a dark theme.
        *   A start screen with 'Start Game', 'Pause', and 'Reset' buttons.
        *   A visible score counter.
        *   Responsive design that fits standard browser windows.
    4.  **Edge Cases & Robustness:**
        *   Implement input buffering to prevent multiple key presses within a single frame causing self-collision.
        *   Ensure the game loop runs at a consistent speed (e.g., 15 FPS).
        *   Handle window resize events gracefully.
    5.  **Code Quality:**
        *   Use ES6+ JavaScript modules/classes structure.
        *   Include detailed comments explaining the game loop and logic.
        *   Ensure the code is minified-ready but readable for debugging.

    **Output Format:**
    Provide the complete source code in a single code block. Ensure the HTML file can be saved and run directly in a browser without any external dependencies."

    *Wait, I need to make sure I am optimizing the PROMPT FOR THE USER'S TASK, not generating the code itself.*
    The user asked me to "improve the user's prompt".
    So my output should be the *refined prompt text* that the user would give to an AI (or in this case, I am acting as the prompt engineer giving them the improved version).
    Actually, looking at the instruction: "Provide only the improved task prompt."
    So I am outputting the prompt that *I* have engineered for the user to use.

    Let's refine the structure to be very explicit about requirements, edge cases, and testing.

    *Revised Plan:*
    1.  Define the role of the AI executing this prompt (Senior Frontend Developer).
    2.  Define the specific task (Create Snake Game).
    3.  Break down into Technical Stack, Game Mechanics, UI/UX, Edge Cases, and Code Standards.
    4.  Define the output format.

    *Self-Correction on "Testing Strategies":* The prompt needs to tell the AI what to test or how to ensure it works.
    *Self-Correction on "Edge Cases":* Explicitly mention input lag, game over states, food spawning logic.

    *Final Polish:* Ensure no markdown headers like "###" if possible, or keep them if they help structure. The instruction says "Just the refined, detailed prompt." Usually, prompts benefit from structure. I will use clear sections.

    Let's write it.

    "Act as a Senior Frontend Developer and Game Designer. Your objective is to generate a complete, self-contained HTML5 Snake game implementation within a single file (HTML + CSS + JavaScript).

    **1. Technical Specifications:**
    - **Rendering Engine:** Use HTML5 Canvas for game rendering to ensure performance.
    - **Language:** ES6+ JavaScript.
    - **Structure:** All CSS and JavaScript must be embedded within the HTML file. No external libraries (like jQuery or Three.js) should be used.
    - **Styling:** Use CSS Flexbox for layout. Implement a dark-themed UI with high contrast for the snake and food.

    **2. Game Mechanics & Logic:**
    - **Grid System:** Define a fixed grid size (e.g., 20x20 tiles).
    - **Movement:** Snake moves continuously based on the last input direction. Implement input buffering to prevent rapid key presses causing self-collision (e.g., pressing 'Up' then 'Left' quickly while moving 'Down').
    - **Food:** Food spawns at random coordinates. It must never spawn on the snake's body.
    - **Growth:** Snake length increases by 1 segment upon eating food.
    - **Collision Detection:**
        - Detect wall collisions (game over).
        - Detect self-collision (game over).
    - **Scoring:** Display current score and high score. Reset score on game over.

    **3. User Interface & Experience:**
    - **Controls:** Arrow keys or WASD for movement. Spacebar for Pause.
    - **Screens:** Include a Start Screen, Playing Screen, and Game Over Screen.
    - **Feedback:** Visual indication when the game is paused.

    **4. Edge Cases & Robustness:**
    - **Input Handling:** Prevent the game state from changing twice in a single tick if keys are pressed rapidly.
    - **Responsiveness:** Ensure the canvas resizes correctly if the window size changes.
    - **Game Loop:** Use `requestAnimationFrame` with a time delta check to maintain a consistent frame rate (approx. 15 FPS).

    **5. Testing & Validation Strategy:**
    - Ensure the code runs without errors in a standard modern browser (Chrome, Firefox, Edge).
    - Verify the snake does not reverse into itself (e.g., cannot move Up if currently moving Down).
    - Verify food does not spawn inside the snake body.
    - Verify the game loop continues correctly after a pause.

    **6. Output Format:**
    - Provide the complete, copy-paste ready HTML code in a single markdown code block.
    - Include comprehensive comments within the JavaScript explaining the game loop and critical logic sections."

    This looks good. It covers all the constraints and requirements. I will remove the numbered lists if they make it look like a document, but structured prompts often use headers. I will keep it structured but ensure it reads as a prompt.

    Wait, I need to make sure I don't add "Here is the prompt". Just the prompt.

    One more check: "Ensure all requirements, edge cases, and testing strategies are explicit."
    I have included them.

    Let's finalize the text.Thinking Process:

1.  **Analyze the Request:**
    *   **Role:** Expert Prompt Engineer.
    *   **Task:** Improve the user's original prompt ("create game snake html") to be extremely clear, detailed, precise, and structured.
    *   **Constraints:** Ensure all requirements, edge cases, and testing strategies are explicit. Keep the original intent intact.
    *   **Output Format:** Provide *only* the improved task prompt. No preamble, no XML tags, no markdown code blocks wrapping the text itself (unless it's part of the prompt, but the instruction says "Just the refined, detailed prompt"). I should output plain text that *is* the prompt.
    *   **Original Intent:** Create a Snake game using HTML (and implied CSS/JS).

2.  **Identify Gaps in Original Prompt:**
    *   "create game snake html" is too vague.
    *   What version of Snake? (Classic?)
    *   What tech stack? (Vanilla JS, React, etc. - assume Vanilla for simplicity unless specified, but better to be specific).
    *   Design requirements? (Responsive, colors, UI).
    *   Functionality? (Controls, score, game over, high score, pause).
    *   Technical constraints? (Single file, external files, specific libraries).
    *   Edge cases? (Wall collision, self-collision, input buffering).
    *   Testing? (How to verify it works).

3.  **Drafting the Improved Prompt:**
    *   *Goal:* Create a comprehensive prompt that a developer (or AI) can use to generate a high-quality Snake game.
    *   *Structure:* Context, Technical Stack, Core Features, UI/UX, Technical Constraints, Edge Cases, Testing/Verification.
    *   *Tone:* Professional, directive, precise.

    *Drafting sections:*
    *   **Role:** You are a senior front-end developer.
    *   **Task:** Build a fully functional Snake game.
    *   **Tech:** HTML5 Canvas, Vanilla JavaScript, CSS3. Single file preferred.
    *   **Features:** Grid-based movement, snake growth, food spawning, score tracking, game over state, pause/resume.
    *   **Controls:** Arrow keys or WASD.
    *   **Design:** Retro aesthetic, responsive canvas.
    *   **Edge Cases:** Prevent 180-degree turns, handle rapid key presses, collision detection.
    *   **Testing:** How to run, expected output.

4.  **Refining for "Extreme Clarity & Detail":**
    *   Instead of just "HTML", specify "HTML5 Canvas".
    *   Instead of "game over", specify "reset logic".
    *   Add specific requirements for code structure (modular vs monolithic).
    *   Explicitly mention "no external libraries" to ensure it's copy-pasteable.
    *   Add specific edge case handling (e.g., input queue).

5.  **Finalizing the Prompt Text:**
    *   Start with a clear objective.
    *   Break down into logical sections (Tech Stack, Core Mechanics, UI/UX, Edge Cases, Deliverables).
    *   Ensure the output is *just* the prompt.

    *Self-Correction on Output Format:* The user said "Provide only the improved task prompt. Do not add any preamble... Just the refined, detailed prompt." This means I should output the text of the prompt itself, not wrap it in markdown code blocks like ``` unless the prompt itself contains them. However, to ensure it's treated as text, I will write it as a structured text block.

    *Wait, one more check:* "Just the refined, detailed prompt." implies I should not output "Here is the prompt:" etc. I will output the content of the optimized prompt directly.

    *Let's make sure the prompt is actionable.* The prompt I generate should tell the AI exactly what to do.

    *Revised Plan:*
    1. Define the context (Classic Snake).
    2. Define the tech stack (HTML/CSS/JS, single file).
    3. Define specific features (Grid, Score, High Score, Controls).
    4. Define technical constraints (Canvas API, Event listeners, No libraries).
    5. Define edge cases (Input buffering, Collision logic).
    6. Define testing criteria (Run in browser, no errors).

6.  **Constructing the Final Prompt:**
    "Act as a Senior Front-End Developer. Your task is to create a fully functional, classic Snake game using only HTML5, CSS3, and Vanilla JavaScript. The final output must be a single, self-contained HTML file.

    **Technical Requirements:**
    - Use HTML5 Canvas for rendering.
    - Use Vanilla JavaScript (ES6+) for logic.
    - Use CSS for styling (responsive design).
    - No external libraries or dependencies.

    **Core Features:**
    - Game Loop: Implement a robust game loop using `requestAnimationFrame` with a fixed frame rate (e.g., 15 FPS).
    - Grid System: Define a tile size (e.g., 20px) and grid dimensions.
    - Snake Logic: Snake grows when food is consumed; moves in the direction of the last input; cannot reverse direction into itself.
    - Food: Randomly spawn food on an empty grid cell.
    - Scoring: Display current score and high score (persisted in localStorage).
    - Game States: Implement Start, Playing, Paused, and Game Over states.

    **Controls:**
    - Support Arrow Keys (Up, Down, Left, Right) and WASD.
    - Prevent rapid key presses that cause self-collision (input buffering).

    **UI/UX:**
    - Centered canvas with a retro aesthetic.
    - Clear on-screen display for Score and High Score.
    - Start/Pause buttons overlay.

    **Edge Cases & Robustness:**
    - Handle window resizing gracefully.
    - Ensure food does not spawn on the snake body.
    - Ensure snake does not reverse direction (e.g., if moving Up, pressing Down should be ignored).
    - Handle keyboard events properly (prevent default scrolling).

    **Testing & Verification:**
    - The code must run without errors in a modern browser.
    - Verify that the game loop maintains consistent speed.
    - Verify that the game ends correctly upon self-collision or wall collision.
    - Verify that the high score is saved and retrieved correctly."

7.  **Final Review:** Does this meet all constraints? Yes. Clear, detailed, structured, explicit requirements/edge cases. No preamble.

    *Wait, I need to make sure I don't output markdown code blocks around the prompt itself unless the prompt asks for it. The instruction says "Just the refined, detailed prompt." I will output the text directly.*

    *Actually, looking at the instruction "Provide only the improved task prompt... Just the refined, detailed prompt." I will output the text of the prompt.*

    *One more thing:* The user wants me to *be* the prompt engineer and output the *improved prompt*. So the output is the prompt text.

    *Let's refine the structure to be very clear.*

    (Self-Correction): Ensure I don't include any conversational filler.

    (Final Polish): Make sure the prompt instructs the AI to generate the code.

    Okay, ready to write.

    *Wait, I should check if I need to output it as a code block.* Usually, prompts are best presented as text. I will output it as plain text to avoid markdown interpretation issues, but structured with headers.

    *Wait, the instruction says "Do not add any preamble... Just the refined, detailed prompt."*
    Okay, I will write the prompt text directly.

    *Wait, one nuance:* "Provide only the improved task prompt."
    So the output should look like:
    "Act as..."
    "..."

    Okay.Thinking Process:

1.  **Analyze the Request:**
    *   **Role:** Expert prompt engineer.
    *   **Task:** Improve the user's original prompt ("create game snake html") to be extremely clear, detailed, precise, and structured.
    *   **Constraints:** Ensure all requirements, edge cases, and testing strategies are explicit. Keep the original intent intact.
    *   **Output Format:** Provide *only* the improved task prompt. No preamble, no XML tags, no explanations.

2.  **Analyze the Original Prompt:**
    *   **Intent:** Create a Snake game.
    *   **Technology:** HTML (implies HTML, CSS, JavaScript).
    *   **Ambiguity:** No specific requirements (e.g., canvas vs. DOM, controls, scoring, difficulty, responsiveness, styling, error handling).
    *   **Missing Details:** Game loop logic, collision detection, food spawning, input handling, visual style, save/load, mobile support.

3.  **Determine Requirements for an "Expert" Improvement:**
    *   **Structure:** Clear sections (Objective, Tech Stack, Features, Constraints, Testing).
    *   **Clarity:** Specific instructions on how the game should work.
    *   **Edge Cases:** What happens when the snake hits itself? What if the window is resized? What about keyboard inputs?
    *   **Testing:** How to verify functionality (unit tests, manual checks).
    *   **Code Quality:** Modern standards, comments, modular structure.

4.  **Drafting the Improved Prompt:**
    *   *Objective:* Build a fully functional Snake game.
    *   *Tech Stack:* HTML5 Canvas, CSS3, Vanilla JavaScript (ES6+).
    *   *Core Mechanics:* Grid-based movement, food spawning, snake growth, collision detection (walls, self).
    *   *UI/UX:* Scoreboard, start/restart button, dark theme.
    *   *Controls:* Arrow keys/WASD, space to pause.
    *   *Responsiveness:* Handle window resize.
    *   *Code Structure:* Single file or modular? Let's aim for a single HTML file for ease of use, but organized clearly.
    *   *Edge Cases:* Prevent reversing direction instantly, handle multiple game loops.
    *   *Testing:* Describe expected behavior for specific scenarios.

5.  **Refining the Prompt (Iterative Process):**
    *   *Draft 1 (Mental):* Make a snake game in HTML. Use canvas. Make it work with arrow keys. Show score. Don't let the snake die on walls. Write clean code. Test it.
    *   *Draft 2 (More detailed):* Create a Snake game using HTML5 Canvas and JavaScript. Include a score counter. Handle collisions with walls and self. Use arrow keys to move. Make sure the code is in one file. Add comments.
    *   *Draft 3 (Expert Level):* Structure it as a specification. Define the grid size, snake speed, food logic. Specify error handling for key presses. Define testing criteria.

6.  **Finalizing the Content (incorporating constraints):**
    *   Need to ensure the prompt asks the AI to *generate* the code based on these specs.
    *   Must explicitly mention edge cases (e.g., snake hitting itself, rapid key presses).
    *   Must mention testing strategies (e.g., verify score updates, verify game over state).

7.  **Constructing the Final Output:**
    *   Start with "Role/Context".
    *   "Task".
    *   "Technical Specifications".
    *   "Game Logic".
    *   "UI/UX".
    *   "Edge Cases & Error Handling".
    *   "Testing & Validation".
    *   "Output Format".

    *Self-Correction on Output:* The user wants *only* the improved prompt. No "Here is the prompt".

    *Review against constraints:* "Ensure all requirements, edge cases, and testing strategies are explicit."

    *Let's write the prompt:*
    "Act as a Senior Frontend Developer. Your task is to generate a complete, self-contained Snake game using HTML5 Canvas, CSS3, and Vanilla JavaScript (ES6+). The code must be contained within a single HTML file.

    **Core Requirements:**
    1. **Game Engine:** Implement a grid-based game loop using `requestAnimationFrame`.
    2. **Visuals:** Use a dark theme with a contrasting color for the snake and food. The grid size must be configurable (e.g., 20x20 pixels per cell).
    3. **Controls:** Support Arrow keys and WASD. Implement input buffering to prevent the snake from reversing into itself within the same tick.
    4. **Game Logic:**
       - Snake grows when eating food.
       - Snake dies on collision with walls or its own body.
       - Food spawns randomly at non-snake coordinates.
    5. **Scoring:** Display a live score counter.

    **Edge Cases & Error Handling:**
    - Handle rapid key presses (input queue) to prevent 180-degree turns.
    - Ensure the game loop does not freeze if the user presses keys too fast.
    - Add a 'Pause' function triggered by the Spacebar.
    - Handle window resize events gracefully without breaking the canvas aspect ratio.

    **Testing & Validation Strategy:**
    - Verify the game starts correctly on page load.
    - Confirm the score increments correctly upon eating food.
    - Test collision logic: Snake must stop moving when hitting walls.
    - Test self-collision logic: Snake must stop moving when hitting its tail.
    - Test pause functionality: Game loop should suspend during pause and resume on Spacebar.
    - Test input handling: Ensure pressing 'Up' then 'Down' simultaneously does not cause a crash.

    **Output Format:**
    Provide the full source code for `index.html` including internal CSS and JavaScript."

    *Refining for maximum precision:* Make it sound like a rigorous prompt for an AI to execute.

    *Final Polish:* Ensure no markdown blocks around the prompt itself unless requested (but usually prompts are text). The instruction says "Provide only the improved task prompt." I will write it as a clear instruction block.

    *Wait, checking the instruction again:* "Provide only the improved task prompt. Do not add any preamble... or XML tags. Just the refined, detailed prompt."

    Okay, I will output the text of the prompt itself.

    *Let's make it even more structured.*

    **Role:** Senior Frontend Developer
    **Task:** Create a high-quality Snake game.
    **Tech:** HTML5 Canvas, Vanilla JS, CSS.
    **Details:** Grid, Loop, Input, Collision, Score.
    **Edge Cases:** Input buffering, Resize, Pause.
    **Testing:** Specific scenarios to validate.

    Okay, ready to generate.

    *Wait, one more check:* "Keep the original intent intact." Original intent: "create game snake html". My improved prompt must still result in a Snake game in HTML. Yes.

    *Final check on constraints:* No preamble. Just the prompt text.

    Let's assemble the final text. I will structure it clearly with headers.

    **Prompt Structure:**
    1. Role & Objective
    2. Technical Stack
    3. Game Mechanics & Logic
    4. UI/UX Specifications
    5. Edge Cases & Error Handling
    6. Testing & Validation Criteria
    7. Output Requirements

    This ensures the "Expert prompt engineer" persona delivers a robust specification.

    *Wait, I need to make sure I don't output markdown code blocks for the prompt itself, just the text of the prompt.* Actually, usually prompts are presented as text. I will write it as a direct instruction block.

    *Wait, looking at "Response Format":* "Provide only the improved task prompt."
    Okay, I will write the prompt content directly.

    *Wait, one nuance:* The user asked me to *improve the user's prompt*. So the output should be the *improved prompt text* that the user would then give to an AI (like me, in this context, or another AI).
    So the output is the *instruction* for the AI to build the snake game.

    Okay, let's write it.

    **Plan:**
    1. Define the role (Senior Frontend Engineer).
    2. Define the task (Build Snake Game).
    3. Detail the tech (HTML/CSS/JS).
    4. Detail the logic (Grid, Loop, Food, Snake).
    5. Detail edge cases (Input buffering, Collision).
    6. Detail testing (Verification steps).
    7. Define output (Single file).

    Let's go.Thinking Process:

1.  **Analyze the Request:**
    *   **Role:** Senior Software Architect / Senior Frontend Developer.
    *   **Task:** Generate a complete, comprehensive implementation plan for a Snake game.
    *   **Constraint:** The output must be a *plan* (descriptive, architectural, strategic). It must *not* include code snippets, pseudocode, or direct implementation commands. It must explain *what* to do, not *how* to write the code (in terms of actual syntax).
    *   **Input:** Detailed task description for a Snake game (HTML5 Canvas, CSS, Vanilla JS, Single File, Specific Mechanics, Testing).
    *   **Output Format:** Clear Markdown plan.
    *   **Key Sections Required:**
        1.  Architecture & Design Choices
        2.  Target File Structure
        3.  Step-by-step Implementation Strategy
        4.  Testing Strategy

2.  **Deconstruct the Task Description:**
    *   **Tech Stack:** HTML5 Canvas, CSS3, Vanilla JS (ES6+). Single `index.html`.
    *   **Architecture:** Modular functions within a single file (Game, Input, Rendering, State).
    *   **Visuals:** Dark theme, Green snake, Red food, Grid background, Responsive canvas.
    *   **Logic:** Grid system (20x20), Game Loop (`requestAnimationFrame`), Movement (1 cell/frame), Food spawning (non-overlap), Scoring (+10), Game Over (wall/self).
    *   **Input:** Arrow/WASD, Input Buffering (prevent reverse), Pause (Space), Restart (Enter/R).
    *   **Edge Cases:** Rapid input handling, Resize handling, Start state, Restart functionality.
    *   **Testing:** Functional tests (movement, collision, score, input buffering, pause).

3.  **Drafting the Plan - Section by Section:**

    *   **Section 1: Architecture & Design Choices**
        *   Need to emphasize the Single Page Application (SPA) nature within a single file.
        *   Modularization: Separate concerns (State Management, Input Handling, Rendering, Game Logic).
        *   Canvas Context: Use 2D context for drawing.
        *   Responsiveness: CSS for layout, JS for resizing canvas logic.
        *   Game Loop: `requestAnimationFrame` with a delta time or frame counter to control speed (though Snake usually moves fixed steps per frame, often 15-30 FPS).
        *   Input Buffering: This is critical. Need a queue mechanism.

    *   **Section 2: Target File Structure**
        *   Since it's a single file, the "structure" is logical organization within `index.html`.
        *   HTML skeleton: `<canvas>`, UI overlays (Score, Game Over, Pause).
        *   CSS: Styles for canvas, overlays, fonts, colors.
        *   JS: Classes or Objects. Let's suggest a Class-based structure for better organization (Game, Snake, Food, InputHandler).

    *   **Section 3: Step-by-step Implementation Strategy**
        *   *Phase 1: Setup & Boilerplate:* HTML structure, Canvas element, CSS reset, basic styling.
        *   *Phase 2: State Management:* Define initial state (snake array, food coordinates, score, game status).
        *   *Phase 3: Input Handling:* Event listeners for keys, implementing the queue logic (store last input, validate against current direction).
        *   *Phase 4: Game Logic:* Movement logic (head update, tail update), Collision detection (walls, self), Food consumption.
        *   *Phase 5: Rendering:* Clear canvas, draw grid, draw snake, draw food, draw UI overlays.
        *   *Phase 6: Game Loop:* Integrate the loop with state updates and rendering.
        *   *Phase 7: Polish:* Responsive resizing, Pause logic, Restart logic.

    *   **Section 4: Testing Strategy**
        *   Manual testing scenarios.
        *   Automated/Console logging verification (simulated).
        *   Specific test cases (Input buffering, Collision, Resize).

4.  **Refining the Constraints:**
    *   **NO CODE SNIPPETS:** I must avoid writing `const canvas = ...` or `function draw()`. I must describe *that* these things happen.
    *   **NO PSEUDOCODE:** Avoid `snake.move(direction)`. Describe the action conceptually.
    *   **Descriptive Only:** Focus on the "Why" and "How" at a high level, not the syntax.

5.  **Review against "Senior Architect" Persona:**
    *   Use professional terminology (State Management, Event Delegation, DOM Manipulation, Context API).
    *   Focus on maintainability and scalability within the constraints of a single file.
    *   Highlight the importance of the Input Buffering mechanism as a design decision to prevent logic errors.

6.  **Final Polish:**
    *   Ensure the Markdown is clean.
    *   Check that all constraints from the prompt are addressed in the plan.
    *   Ensure the "No Code" rule is strictly followed.

    *(Self-Correction during drafting)*: I initially thought about suggesting a separate `game.js` file, but the prompt explicitly says "The final output must be a single `index.html` file containing all necessary code." So the file structure section must reflect that everything lives in one file, organized logically (e.g., `<style>`, `<body>`, `<script>`).

    *(Refinement on Testing)*: Since this is a plan for generating code, the testing strategy should describe how the *generated code* will be tested, or how the architect verifies the logic during the implementation phase. The prompt asks for a "Testing Strategy (how to verify each component)". I will frame this as how the developer will verify the implementation logic.

    *(Refinement on Input Buffering)*: Explain the mechanism conceptually (e.g., "maintain a stack of pending inputs").

    *(Refinement on Responsiveness)*: Explain the logic of calculating cell size based on window width/height and scaling the internal grid.

7.  **Final Output Generation** (incorporating all thoughts).
    *   Start with Architecture.
    *   Move to File Structure.
    *   Detail Implementation Steps.
    *   Detail Testing Steps.
    *   Ensure no code blocks are present.

    *Wait, one more check:* "Do not include code snippets, pseudocode, or direct implementation commands."
    *   Example of violation: `const snake = [{x:10, y:10}]` -> **Violation**.
    *   Example of violation: `if (key === 'ArrowUp')` -> **Violation**.
    *   Correct approach: "Define an array representing the snake's body segments." "Check if the key event matches the Up arrow key."

    Let's write the response.

    *Wait, re-reading the prompt:* "Provide the full source code for `index.html`." is listed under "Output Requirements" in the *Detailed Task Description*, but the *Response Format* instruction says "Provide the complete plan in clear Markdown. No conversational preamble."
    *   Wait, the "Detailed Task Description" says "Output Requirements: Provide the full source code for `index.html`."
    *   BUT the "Response Format" instruction at the very top says "Provide the complete plan in clear Markdown. No conversational preamble." AND "Important: The plan should be descriptive only. Do not include code snippets..."
    *   This is a conflict. The user instruction at the very top says "You are a software architect assistant. Based on the following detailed task description, generate a complete, comprehensive implementation plan."
    *   The "Detailed Task Description" contains the actual task (Act as Senior Frontend Developer... generate a complete, self-contained Snake game...).
    *   However, the *Response Format* instruction overrides the "Detailed Task Description" regarding the output format. The user explicitly asked for a *Plan* in the "Response Format" section ("Provide the complete plan in clear Markdown").
    *   The "Important" constraint says "The plan should be descriptive only. Do not include code snippets...".
    *   So, I must NOT write the code. I must write the PLAN for how to write the code.
    *   Okay, I will follow the Response Format instructions strictly. No code. Just the plan.

    *Wait, looking closer at the "Detailed Task Description" Output Requirements:* "Provide the full source code for `index.html`."
    *   Looking at the "Response Format": "Provide the complete plan in clear Markdown."
    *   Looking at the "Important": "The plan should be descriptive only. Do not include code snippets..."
    *   This is a classic "System Instruction vs User Prompt" conflict. The System Instruction (Response Format) dictates the output type (Plan), while the User Prompt (Detailed Task Description) asks for Code.
    *   Given the "Important" constraint ("Do not include code snippets"), I must prioritize the Response Format instruction which explicitly forbids code. I will provide the Plan as requested in the Response Format section, detailing the architecture and strategy to build the game, without writing the actual code.

    *Wait, one more nuance:* "Do not include code snippets, pseudocode, or direct implementation commands."
    *   This confirms I should describe the *implementation*, not provide the *implementation*.
    *   Okay, I will generate the Implementation Plan.

    *Wait, checking the "Response Format" again:* "Provide the complete plan in clear Markdown. No conversational preamble."
    *   Okay, I will start directly with the plan.

    *Wait, checking the "Detailed Task Description" again:* "Output Requirements: Provide the full source code for `index.html`."
    *   This is inside the task description. But the System Instruction (Response Format) says "Provide the complete plan... Do not include code snippets".
    *   Usually, the System Instruction (Response Format) takes precedence for the *format* of my response. The "Detailed Task Description" sets the *content* of the plan.
    *   I will follow the "Response Format" constraint (Plan, No Code) because the "Important" constraint explicitly says "Do not include code snippets". If I were to provide the code, I would be violating the "Important" constraint.
    *   So, Plan it is.

    *Wait, checking the "Response Format" instruction again:* "Provide the complete plan in clear Markdown."
    *   Okay, I am confident. I will write the plan.

    *Wait, checking the "Detailed Task Description" again:* "Act as a Senior Frontend Developer. Your objective is to generate a complete, self-contained Snake game..."
    *   This implies the user wants the code. But the "Response Format" says "Provide the complete plan".
    *   This is a "Plan Generation" task. I need to explain *how* to build it.
    *   Okay, I will proceed with the Plan.

    *Wait, I need to make sure I don't violate the "No code" rule.*
    *   I will describe the components without using syntax.
    *   I will describe the logic flow without using `if/else` blocks.

    *Let's structure the plan.*
    1. Architecture & Design Choices
    2. Target File Structure
    3. Step-by-step Implementation Strategy
    4. Testing Strategy

    *Let's ensure I cover all points from the Detailed Task Description.*
    *   Tech Stack: HTML5, CSS, JS.
    *   Modular: Game, Input, Rendering, State.
    *   Responsive: Canvas resize.
    *   Mechanics: Grid, Loop, Movement, Food, Score, Game Over.
    *   Controls: Keyboard, Buffering, Pause, Restart.
    *   Edge Cases: Rapid input, Resize, Start, Restart.
    *   Testing: Functional, Collision, Score, Input, Pause.

    *Okay, ready to write.*Thinking Process:

1.  **Analyze the Request:**
    *   Role: Technical Project Manager Assistant.
    *   Task: Break down the provided implementation plan into a list of specific, sequential, actionable steps.
    *   Constraints:
        *   Clear, single objective per step.
        *   Numbered sequentially (1., 2., 3.).
        *   **CRITICAL:** Extremely concise, at most 3 to 5 high-level sequential milestones.
        *   **CRITICAL:** Do NOT create micro-steps (e.g., editing a single line). Group related activities into broad phases.
        *   Fewer steps are preferred.
        *   Output Format: ONLY the numbered list. No preamble, no other text.

2.  **Analyze the Input Plan:**
    *   The input plan details a Single-File Snake Game.
    *   It covers Architecture, File Structure, Implementation Phases (Foundation, State, Input, Logic, Rendering, Polish), and Testing.
    *   The input plan actually has 6 implementation phases and a testing section.
    *   However, the constraint says "at most 3 to 5 high-level sequential milestones".
    *   I need to consolidate the 6 phases + testing into 3-5 broad milestones.

3.  **Drafting Milestones:**
    *   *Idea 1:* Setup HTML/CSS/JS structure.
    *   *Idea 2:* Implement core game logic (state, input, movement, collision).
    *   *Idea 3:* Implement rendering and game loop.
    *   *Idea 4:* Test and finalize.

    *   *Refinement for "3 to 5 high-level sequential milestones":*
        *   Milestone 1: Setup Foundation (HTML/CSS/Config).
        *   Milestone 2: Implement Core Logic (State/Input/Physics).
        *   Milestone 3: Integrate Rendering & Loop.
        *   Milestone 4: Verification & Polish.

    *   *Constraint Check:* "Group related activities together into broad phases (for example: Phase 1: Implement HTML/CSS structure and core JavaScript logic; Phase 2: Run verification and fix bugs; Phase 3: Finalize)".
    *   The prompt asks for "at most 3 to 5 high-level sequential milestones".
    *   Let's try to condense into 3 or 4 very clear steps.

    *   *Draft 1:*
        1. Set up single-file HTML structure with CSS styling and JavaScript configuration constants.
        2. Implement core game logic including state management, input buffering, movement, and collision detection.
        3. Integrate the HTML5 Canvas rendering engine and main game loop.
        4. Conduct functional testing and fix bugs related to responsiveness and edge cases.

    *   *Draft 2 (More concise):*
        1. Initialize project structure with HTML/CSS layout and JavaScript configuration constants.
        2. Implement core game mechanics including state management, input buffering, and collision logic.
        3. Integrate the rendering engine and requestAnimationFrame game loop.
        4. Perform end-to-end testing and finalize UI polish.

    *   *Review against constraints:*
        *   Concise? Yes.
        *   3-5 steps? Yes (4 steps).
        *   No micro-steps? Yes.
        *   Sequential? Yes.
        *   Only numbered list? Yes.

    *   *Refining for maximum conciseness and clarity:*
        1. Establish the single-file HTML structure with CSS styling and JavaScript configuration constants.
        2. Implement core game logic including state management, input buffering, and collision detection.
        3. Integrate the Canvas rendering engine and main game loop.
        4. Execute verification tests and finalize UI polish.

    *   *Wait, looking at the "Phase 1: Foundation and Setup" through "Phase 6: Polish" in the input, I need to collapse these into 3-5.*
    *   Let's try to make it 3 steps to be safe on the "fewer steps are highly preferred" instruction, but the constraint says "at most 3 to 5". So 3 is fine.
    *   Let's try 4 steps to ensure logical separation (Setup, Logic, Render, Test).

    *   *Final Polish:*
        1. Initialize the single-file HTML structure with CSS styling and JavaScript configuration constants.
        2. Implement core game logic including state management, input buffering, and collision detection.
        3. Integrate the Canvas rendering engine and main game loop.
        4. Execute verification tests and finalize UI polish.

    *   *Wait, checking the "Phase 1" to "Phase 6" in the input again.*
        *   Phase 1: Foundation (HTML/CSS/Config)
        *   Phase 2: State Init
        *   Phase 3: Input
        *   Phase 4: Logic
        *   Phase 5: Render/Loop
        *   Phase 6: Polish
        *   Testing is separate.

    *   To combine into 3-5 milestones:
        1.  Setup Foundation: Create HTML/CSS layout and JS configuration constants.
        2.  Implement Core Logic: Build state management, input buffering, movement, and collision detection.
        3.  Integrate Rendering: Connect the Canvas engine and game loop.
        4.  Verify and Polish: Run tests and fix bugs.

    *   Let's make them strictly follow the "Single objective" rule.
    *   1. Create single-file HTML structure with CSS styling and JavaScript configuration constants.
    *   2. Implement core game logic including state management, input buffering, and collision detection.
    *   3. Integrate the CanvasThinking Process:

1.  **Analyze the Request:**
    *   Role: Technical Project Manager Assistant.
    *   Task: Break down the provided Implementation Plan into a list of specific, sequential, actionable steps.
    *   Constraints:
        *   Clear, single objective per step.
        *   Numbered sequentially (1., 2., 3.).
        *   **CRITICAL:** Extremely concise, at most 3 to 5 high-level sequential milestones.
        *   **CRITICAL:** Do not create micro-steps (no micro-details like "edit a single line").
        *   Group related activities into broad phases.
        *   Fewer steps are highly preferred.
        *   Output Format: ONLY the numbered list. No preamble, no other text.

2.  **Analyze the Input (Implementation Plan):**
    *   The plan describes building a Single-File Snake Game.
    *   It covers: Architecture (HTML/CSS/JS), State Management, Rendering, Input Handling, Game Logic, and Testing.
    *   The detailed steps in the input plan (Phase 1 to Phase 6 + Testing) are too granular. They describe specific phases like "Create HTML skeleton", "Implement logic", "Test functionality".
    *   I need to condense this into 3-5 high-level milestones.

3.  **Drafting Steps (Iterative Process):**

    *   *Attempt 1:*
        1. Set up HTML structure, CSS styles, and JS configuration constants.
        2. Implement state management, input handling, and core game logic (movement, collision, score).
        3. Build the rendering engine and game loop using requestAnimationFrame.
        4. Add responsiveness and UI polish.
        5. Run functional and collision tests.
        *Critique 1:* A bit generic, but fits the "milestone" style. Maybe too many steps? 5 is the max. Let's try to merge.

    *   *Attempt 2:*
        1. Scaffold HTML structure, CSS styling, and JavaScript configuration constants.
        2. Implement state management, input buffering, and core game logic including movement and collision detection.
        3. Integrate the rendering engine and main game loop with responsive canvas handling.
        4. Verify functionality through testing for movement, collisions, input robustness, and resizing.
        *Critique 2:* This is 4 steps. It captures the essence.
        Let's look at the "Phase" structure in the input:
        Phase 1: Foundation (HTML/CSS/Config)
        Phase 2: State/Logic (State/Input/Logic)
        Phase 3: Rendering/Loop (Render/Loop)
        Phase 4: Polish/Testing (Polish/Test)
        
        Actually, I can group Phase 1 & 2, 2 & 3, and 3 & 4 to get 3 steps.
        Or keep 4 steps if it flows well. The requirement says "at most 3 to 5". 4 or 5 is safer to ensure coverage without being too vague. Let's aim for 4 distinct high-level milestones.

    *   *Attempt 3 (Refining for "Single Objective" and "Actionable"):*
        1. Establish the HTML5/CSS/JS foundation including responsive canvas configuration and UI overlays.
        2. Implement core game state management, input buffering, and collision/movement logic.
        3. Integrate the rendering engine and game loop with dynamic resizing capabilities.
        4. Execute verification testing for game mechanics, input robustness, and responsive behavior.

    *   *Review against constraints:*
        *   Concise? Yes.
        *   Numbered? Yes.
        *   3-5 milestones? Yes (4 steps).
        *   No micro-steps? Yes (no "create canvas element", just "Establish foundation").
        *   No preamble? Yes.

    *   *Final Polish:* Ensure the steps flow logically from setup -> logic -> render -> test.
        1. Scaffold the single-file HTML structure with CSS styling and JS configuration constants.
        2. Implement state management, input buffering, and core game logic for movement and collision detection.
        3. Integrate the rendering engine and game loop with dynamic canvas resizing support.
        4. Verify functionality through testing for mechanics, input robustness, and responsive behavior.

    *   *Wait, let's check the "Single Objective" constraint again.*
        Step 1: Setup (HTML/CSS/Config).
        Step 2: Logic (State/Input/Move/Coll).
        Step 3: Render (Engine/Loop/Resize).
        Step 4: Test (Verification).
        This looks good. I will combine Testing into the final phase or keep it separate? The input plan has a specific "Testing Strategy" section. It might be better to have 3 main phases to keep it very concise as per "Fewer steps are highly preferred". Let's try 3 steps.

    *   *Attempt 3 (3 Steps):*
        1. Scaffold the single-file HTML structure with CSS styling, JS configuration, and UI overlays.
        2. Implement state management, input buffering, game logic, and the rendering engine with a responsive loop.
        3. Execute verification testing for game mechanics, input robustness, and responsive behavior.
        
        *Critique:* Step 2 is a bit long but covers "Phase 2, 3, 4" of the input plan. It fits the "group related activities" requirement.
        Let's try to split Step 2 slightly to make it clearer, or keep it tight. The prompt says "at most 3 to 5". 3 is the minimum I should probably hit to be "extremely concise". But 4 is often clearer for PMs. Let's go with 4 to ensure the "Polish/Testing" phase isn't swallowed.
        Actually, looking at "Phase 1: Foundation", "Phase 2: State", "Phase 3: Render", "Phase 4: Polish".
        I can merge 2 & 3 into "Core Game Engine" and keep 1, 2+3, 4?
        Let's stick to 4 steps. It's within the limit (3-5) and provides better granularity without being micro.

    *   *Refining Step 1:* Scaffold single-file HTML structure with CSS styling, JS configuration, and UI overlays.
    *   *Refining Step 2:* Implement state management, input buffering, and core game logic for movement and collision detection.
    *   *Refining Step 3:* Integrate the rendering engine and main game loop with dynamic canvas resizing support.
    *   *Refining Step 4:* Execute verification testing for game mechanics, input robustness, and responsive behavior.

    *   *Wait, one more check:* "Each step must... Be clear and focus on a single objective".
    Step 2 mixes State, Input, and Logic. Is that one objective? Yes, "Implement Core Game Engine".
    Step 3 mixes Render and Loop. Yes, "Implement Rendering Engine".
    Step 4 is Testing. Yes.

    Let's finalize the wording for maximum clarity and conciseness.

    1. Scaffold the single-file HTML structure with CSS styling, JavaScript configuration, and UI overlays.
    2. Implement state management, input buffering, and core game logic including movement and collision detection.
    3. Integrate the rendering engine and main game loop with dynamic canvas resizing support.
    4. Execute verification testing for game mechanics, input robustness, and responsive behavior.

    Actually, I can condense thisThinking Process:

1.  **Analyze the Request:**
    *   Role: Technical Project Manager Assistant.
    *   Task: Break down the provided Implementation Plan into a list of specific, sequential, actionable steps.
    *   Constraint 1: Each step must be clear, focus on a single objective, and be numbered sequentially.
    *   Constraint 2: CRITICAL REQUIREMENT - The list of steps must be *extremely concise* and contain *at most 3 to 5 high-level sequential milestones*. Do not create micro-steps. Group related activities into broad phases.
    *   Constraint 3: Fewer steps are highly preferred.
    *   Constraint 4: Provide ONLY the numbered list. No preamble.

2.  **Analyze the Input (Implementation Plan):**
    *   The plan describes building a Single-File Snake Game.
    *   It covers Architecture (Single HTML file, modular JS, state management), File Structure (HTML/CSS/JS sections), and a detailed Step-by-Step Implementation Strategy (Phases 1-6) plus Testing Strategy.
    *   The detailed plan has 6 phases + testing.
    *   I need to condense this into 3-5 high-level milestones.

3.  **Drafting the Milestones (Iterative Process):**

    *   *Attempt 1:*
        1. Setup HTML/CSS/JS structure.
        2. Implement state and input handling.
        3. Build game logic and movement.
        4. Create rendering and game loop.
        5. Test functionality and fix bugs.
        *Critique 1:* This is 5 steps, fits the constraint (3-5). It's concise. It covers setup, logic, rendering, and testing.

    *   *Attempt 2 (Grouping closer to the phases in the text):*
        1. Create single-file architecture with HTML canvas and dark theme CSS.
        2. Initialize game state, input queue, and movement logic.
        3. Implement rendering engine and game loop with collision detection.
        4. Verify functionality through manual testing of movement, scoring, and edge cases.
        *Critique 2:* 4 steps. Very clean. Covers structure, core logic, rendering, and testing.

    *   *Attempt 3 (Focusing on the "3-5 high-level sequential milestones" requirement):*
        1. Establish single-file foundation with responsive canvas and CSS styling.
        2. Develop state management, input buffering, and core movement logic.
        3. Build rendering engine, game loop, and collision detection systems.
        4. Execute functional testing for gameplay mechanics and edge cases.

    *   *Attempt 4 (Refining for "At most 3 to 5"):*
        Let's try to merge Testing into the build phase or keep it separate if it counts as a milestone. The prompt says "Group related activities together into broad phases".
        Let's aim for 3 steps to be "extremely concise".
        1. Setup environment and core structure.
        2. Implement logic and rendering.
        3. Test and finalize.

        But the prompt asks for "at most 3 to 5". So 3, 4, or 5 is okay. Let's go with 4 to capture the distinct phases of the original plan (Setup, Logic, Render/Test). Actually, the original plan has 6 implementation phases + testing. I need to condense that significantly.

        *Revised Draft:*
        1.  Initialize single-file HTML architecture with responsive canvas and CSS styling.
        2.  Implement state management, input buffering, and core game logic.
        3.  Construct the rendering engine, game loop, and collision detection.
        4.  Perform functional testing for movement, scoring, and resizing behaviors.

        This looks good. 4 steps. Fits the constraints.

4.  **Final Review against Constraints:**
    *   Numbered sequentially? Yes.
    *   Clear, single objective per step? Yes.
    *   At most 3 to 5 high-level sequential milestones? Yes (4 steps).
    *   No micro-steps? Yes.
    *   Provide ONLY the numbered list? Yes.

5.  **Final Polish:** Ensure the language is imperative and direct.

    1. Establish single-file HTML architecture with responsive canvas and dark theme CSS.
    2. Implement state management, input buffering, and core game logic.
    3. Construct the rendering engine, game loop, and collision detection.
    4. Execute functional testing for gameplay mechanics and edge cases.

    Wait, "Execute functional testing" might be too specific if I can group it. But the plan separates implementation and testing strategy. Let's keep it to ensure quality.

    Actually, looking at "At most 3 to 5", I can condense further to 3 steps to be "extremely concise".
    1. Set up single-file foundation with responsive canvas and CSS.
    2. Develop game logic including state management, input handling, and rendering.
    3. Verify functionality through comprehensive testing.

    This is 3 steps. Fits "at most 3 to 5". It minimizes transitions. Let's use this approach.

    Wait, let's look at the instruction again: "Fewer steps are highly preferred to minimize transitions."
    So 3 is better than 4.

    Step 1: Setup (Architecture + CSS)
    Step 2: Core Implementation (Logic + Rendering + Loop)
    Step 3: Validation (Testing)

    Let's refine Step 2 to be specific enough to distinguish from 1.

    1. Establish single-file HTML architecture with responsive canvas and dark theme CSS.
    2. Develop state management, input buffering, rendering engine, and game logic.
    3. Execute functional testing for gameplay mechanics, collisions, and responsiveness.

    This meets all criteria.

    Wait, I need to make sure I don't add preamble. Just the list.

    Let's double check the "3 to 5" constraint. "At most 3 to 5". So 3, 4, or 5. 3 is the minimum allowed by the "at most 3" phrasing? No, "at most 3 to 5" is slightly ambiguous. Usually means "between 3 and 5 inclusive". But "Fewer steps are highly preferred". So 3 is the best target.

    Let's write it.

    1. Establish single-file HTML architecture with responsive canvas and dark theme CSS.
    2.