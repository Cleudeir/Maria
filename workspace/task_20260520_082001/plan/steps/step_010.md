# Step 10

Approved & Executed: write_file {'path': 'output/snake_game/config.py', 'content': '"""\nConfiguration Module for Snake Game.\n\nThis module contains all game constants and settings.\n"""\n\nfrom enum import Enum\nfrom typing import Tuple, Optional\n\n\nclass GameState(Enum):\n    """Enumeration of possible game states."""\n    INITIALIZED = "initialized"\n    RUNNING = "running"\n    PAUSED = "paused"\n    GAME_OVER = "game_over"\n\n\n# Screen dimensions\nSCREEN_WIDTH = 640\nSCREEN_HEIGHT = 480\n\n# Grid configuration\nGRID_WIDTH = 20\nGRID_HEIGHT = 15\nCELL_SIZE = 32\n\n# Colors\nCOLOR_BLACK = (0, 0, 0)\nCOLOR_WHITE = (255, 255, 255)\nCOLOR_GREEN = (0, 255, 0)\nCOLOR_GREEN_DARK = (0, 128, 0)\nCOLOR_RED = (255, 0, 0)\nCOLOR_RED_DARK = (128, 0, 0)\nCOLOR_YELLOW = (255, 255, 0)\nCOLOR_YELLOW_DARK = (128, 128, 0)\n\n# Game speed (frames per second)\nINITIAL_SPEED = 60\n\n\ndef get_snake_color() -> Tuple[int, int, int]:\n    """Get the snake color."""\n    return COLOR_GREEN\n\n\ndef get_food_color() -> Tuple[int, int, int]:\n    """Get the food color."""\n    return COLOR_RED\n\n\ndef get_grid_color() -> Tuple[int, int, int]:\n    """Get the grid color."""\n    return COLOR_WHITE\n'}

Success: File 'output/snake_game/config.py' written successfully.
