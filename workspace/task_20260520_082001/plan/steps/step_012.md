# Step 12

Approved & Executed: write_file {'path': 'output/snake_game/controllers.py', 'content': '"""\nControllers Module for Snake Game.\n\nThis module handles input processing and game logic.\n"""\n\nfrom typing import Optional, Tuple, List\n\nfrom snake_game.config import (\n    GameState,\n    CELL_SIZE,\n    COLOR_WHITE,\n    GRID_WIDTH,\n    GRID_HEIGHT,\n)\nfrom snake_game.models import Direction, Snake, Food\n\n\nclass GameController:\n    """\n    Controls the game logic and input handling.\n    \n    Responsibilities include:\n    - Processing keyboard input\n    - Managing game state transitions\n    - Collision detection\n    - Score tracking\n    """\n    \n    def __init__(self, snake: Snake = None, food: Food = None):\n        """\n        Initialize the game controller.\n        \n        Args:\n            snake: The Snake instance\n            food: The Food instance\n        """\n        self.snake = snake if snake else Snake()\n        self.food = food if food else Food(self.snake)\n        self.score = 0\n        self.game_state = GameState.INITIALIZED\n        \n        # Prevent 180-degree turns in same frame\n        self.last_input_direction = Direction.RIGHT\n    \n    def process_input(self) -> Tuple[bool, bool]:\n        """\n        Process keyboard input and handle movement.\n        \n        Returns:\n            Tuple of (game_over, paused) booleans\n        """\n        game_over = False\n        paused = False\n        \n        # Check for input events\n        if self.game_state == GameState.RUNNING:\n            # Get current direction\n            current_direction = self.snake.direction\n            \n            # Check if we need to change direction\n            # Use input buffer to handle rapid inputs\n            if len(self.input_buffer) > 0:\n                new_direction = self.input_buffer.pop(0)\n            else:\n                # No input buffer, check for new input\n                pass\n            \n            # Check for valid direction change\n            if self.snake.set_direction(new_direction):\n                # Check for 180-degree turn (reversing)\n                if (current_direction[0] + new_direction[0] == 0 and \n                    current_direction[1] + new_direction[1] == 0):\n                    # Cancel the direction change\n                    new_direction = Direction.RIGHT\n            \n            # Process movement\n            moved = self.snake.move()\n            \n            # Check for collision\n            if not moved:\n                game_over = True\n            else:\n                # Check if eating food\n                if self.snake.get_head_position() == self.food.get_position():\n                    self.snake.grow()\n                    self.score += 1\n                    self.food = Food(self.snake)\n        \n        elif self.game_state == GameState.PAUSED:\n            # Resume game on space press\n            if len(self.input_buffer) > 0:\n                new_direction = self.input_buffer.pop(0)\n                current_direction = self.snake.direction\n                if self.snake.set_direction(new_direction):\n                    moved = self.snake.move()\n                    if not moved:\n                        game_over = True\n                    elif self.snake.get_head_position() == self.food.get_position():\n                        self.snake.grow()\n                        self.score += 1\n                        self.food = Food(self.snake)\n        \n        return game_over, paused\n    \n    def check_collisions(self) -> bool:\n        """\n        Check for game over conditions (wall or self collision).\n        \n        This should be called after each move to determine if the game ends.\n        \n        Returns:\n            True if collision detected, False otherwise\n        """\n        head = self.snake.get_head_position()\n        \n        # Check wall collision\n        if (head[0] < 0 or head[0] >= GRID_WIDTH or \n            head[1] < 0 or head[1] >= GRID_HEIGHT):\n            return True\n        \n        # Check self collision\n        # Skip the tail segment since it will move\n        for i, segment in enumerate(self.snake.body[:-1]):\n            if segment == head:\n                return True\n        \n        return False\n    \n    def handle_game_over(self, game_over: bool):\n        """\n        Handle game over conditions.\n        \n        Args:\n            game_over: Whether the game has ended\n        """\n        if game_over:\n            self.game_state = GameState.GAME_OVER\n    \n    def set_game_state(self, state: GameState):\n        """\n        Set the current game state.\n        \n        Args:\n            state: The new game state to transition to\n        """\n        self.game_state = state\n    \n    def handle_high_score(self, score: int):\n        """\n        Update high score if current score exceeds it.\n        \n        Args:\n            score: The current score to check against\n        """\n        # High score logic would go here\n        # In a real implementation, this would read/write to a file\n        pass\n'}

Success: File 'output/snake_game/controllers.py' written successfully.
