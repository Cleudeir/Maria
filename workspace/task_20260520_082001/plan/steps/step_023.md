# Step 23

Approved & Executed: write_file {'path': 'output/snake_game/controllers.py', 'content': '"""\nControllers Module for Snake Game.\n\nThis module handles game logic and state management.\n"""\n\nfrom typing import Tuple\nimport random\n\nfrom snake_game.config import GRID_WIDTH, GRID_HEIGHT\nfrom snake_game.models import Snake, Food, Direction\n\n\nclass GameController:\n    """\n    Handles all game logic and state management.\n    \n    Responsibilities include:\n    - Managing game state\n    - Processing game input\n    - Updating game rules\n    """\n    \n    def __init__(self):\n        """Initialize the game controller."""\n        self.game_state = "RUNNING"\n        self.score = 0\n    \n    def check_collision(self, snake: Snake) -> bool:\n        """\n        Check if the snake has collided with itself.\n        \n        Args:\n            snake: The snake object to check\n            \n        Returns:\n            bool: True if collision detected, False otherwise\n        """\n        head = snake.body[0]\n        for segment in snake.body[1:]:\n            if head == segment:\n                return True\n        return False\n    \n    def check_food_collision(self, snake: Snake, food: Food) -> bool:\n        """\n        Check if the snake has eaten the food.\n        \n        Args:\n            snake: The snake object\n            food: The food object\n            \n        Returns:\n            bool: True if collision detected, False otherwise\n        """\n        head = snake.body[0]\n        return head == food.get_position()\n    \n    def handle_game_over(self):\n        """Handle game over conditions."""\n        self.game_state = "GAME_OVER"\n    \n    def handle_pause(self):\n        """Handle pause conditions."""\n        pass\n    \n    def reset_game(self):\n        """Reset the game to initial state."""\n        self.score = 0\n        self.game_state = "RUNNING"\n        self._reset_snake()\n    \n    def _reset_snake(self):\n        """Reset the snake to initial state."""\n        snake = Snake()\n        snake.body = [(1, 1), (0, 1), (0, 0), (-1, 1)]\n        snake.direction = Direction.RIGHT\n'}

Success: File 'output/snake_game/controllers.py' written successfully.
